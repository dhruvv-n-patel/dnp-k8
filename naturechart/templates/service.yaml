apiVersion: v1
kind: Service
metadata:
  name: {{ include "naturechart.fullname" . }}
  labels:
    {{- include "naturechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nature-client.selectorLabels" . | nindent 4 }}

